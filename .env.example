RELAY_MODE=live
RELAY_SEED=0
RELAY_AUTH_TOKENS=replace-with-random-operator-token:relay-operator:operator
# Optional: issue intake and reporter replies are Devin's own GitHub connection
# (github:issues / github:issue_comment automations). Relay's webhook never
# enrolls issues; it only adds PR/review events for issues Relay already tracks.
GITHUB_WEBHOOK_SECRET=replace-with-random-webhook-secret
GITHUB_TOKEN=replace-with-minimally-scoped-github-token
GITHUB_DEFAULT_BRANCH=master
# Controller gate: the allowed repository is fixed to exloong/superset in code.
# Legacy webhook-path gate only: Devin's automation fires on every opened issue.
RELAY_TRUSTED_LABEL=bug
# Optional: comma-separated GitHub logins allowed to trigger enrollment.
RELAY_TRUSTED_ACTORS=
# Devin service-user key (used by the worker to provision Relay's two Devin
# Automations and poll their sessions read-only, and by the API to proxy
# automation management). Needs ManageOrgAutomations,
# ViewOrgAutomations and ViewOrgSessions. Never sent to the browser.
DEVIN_API_TOKEN=replace-with-devin-api-token
DEVIN_ORG_ID=org-replace-with-your-organization-id
DEVIN_REVIEW_TOKEN=
APP_PORT=4173
